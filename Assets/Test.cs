using UnityEngine; using System.Collections;   public class Boss {         	private int hp = 100;          // 体力 	private int mp = 53;     // MP 	private int mp_power = 50;     // 魔法攻撃力 	private int mp_use = 5;     // 魔法使用時MP消費 	private int power = 25; // 攻撃力  	//魔法攻撃の関数 	public void MagicAttack() { 		if (mp >= mp_use) { 			mp -= mp_use; 			Debug.Log (this.mp_power + "ダメージの魔法攻撃をした。"); 			Debug.Log ("残りMPは" + mp.ToString ()); 		} else { 			Debug.Log ("MPがたりません"); 		} 	} }   public class Test : MonoBehaviour { 	void Start () { 		// Bossクラスの変数を宣言してインスタンスを代入 		Boss lastboss = new Boss ();  	 		for (int i = 0 ; i < 11; i++) { 		lastboss.MagicAttack(); 	} 				// 要素数5の配列を初期化する 				int[] array = new int[5];  				// 配列の各要素に値を代入する 				array  [0] = 30; 				array  [1] = 20; 				array  [2] = 50; 				array  [3] = 10; 				array  [4] = 80;  				// 配列の要素をすべて表示する 				for (int i = 4; i >= 0; i--) { 					Debug.Log (array  [i]); 				} 			}  			// Update is called once per frame 			void Update () {  			} 		} 	 